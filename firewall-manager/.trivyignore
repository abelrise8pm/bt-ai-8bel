# Alpine base image CVEs that are accepted risks or false positives
# Add CVE IDs here if needed after reviewing Trivy scan results
# Format: CVE-YYYY-NNNNN (one per line)

# CVE-2026-22184 - CRITICAL (NIST) / MEDIUM (CNA) - Disputed
# zlib contrib/untgz demo utility buffer overflow. Not exploitable:
# affects only the untgz CLI tool, not the core zlib library.
# Firewall-manager has no interactive access or untgz binary.
# Suppress until Alpine releases zlib 1.3.2-r0, then remove.
CVE-2026-22184
